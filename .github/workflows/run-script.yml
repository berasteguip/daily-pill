name: Run Python script and persist history

on:
  schedule:
    - cron: "0 21 * * *"   # everyday at 21:00
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Ensure history file exists
        run: |
          if [ ! -f history.txt ]; then
            touch history.txt
          fi

      - name: Run script
        working-directory: ./src
        run: python main.py

      - name: Commit and push topics
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          git add material/topics/*.csv

          # If no changes, do not fail the job
          git diff --cached --quiet && exit 0

          git commit -m "Update topics"
          git push
